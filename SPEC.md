# アプリ仕様書：課題ログ（Kadai Log）

## 1. 概要

「課題ログ」は、日々の仕事や活動の中で直面した課題を記録し、
README.md に掲げる開発方針（課題を明らかにする → なぜ解決するかを論じる →
選択肢を比較する → 理由を選ぶ → 結果を検証する）に沿って一つひとつ振り返るための
個人用ツールである。

対象は開発案件に限らず、業務・学習・創作など「課題を立てて解決する」あらゆる活動。
記録を溜めることで、自分がどう考え、どう動いたかを後から言葉で説明できる状態を作る。

## 2. 背景・課題

- 日々の判断や試行錯誤は、記録しなければすぐに忘れる。
- 「十分に理解」とは自分の言葉で説明できることだが、その場では理解した気になっても
  時間が経つと説明できなくなることが多い。
- 課題解決のプロセス（課題→理由→選択肢→決定→検証）を都度言語化する習慣がないと、
  同じ失敗や堂々巡りの検討を繰り返しやすい。

## 3. 目的

1. 課題とその解決プロセスを、決まった型（テンプレート）で記録できるようにする。
2. 記録を後から検索・振り返りできるようにし、過去の意思決定を再利用できるようにする。
3. 「方針はアップデートされる」という考え方に基づき、記録自体も後から追記・修正できるようにする。

## 4. 対象ユーザー

- リポジトリ所有者本人（個人利用）を主な想定ユーザーとする。
- マルチユーザー機能は初期スコープに含めない。

## 5. 機能要件

### 5.1 課題エントリの作成・編集
各エントリは以下の項目を持つ（README の開発方針 1〜5 に対応）。

| 項目 | 内容 |
|---|---|
| タイトル | 課題の要約（一行） |
| 課題 | 何が問題か |
| 理由 | なぜその課題を解決する必要があるか |
| 選択肢 | 検討した複数の手法とその違い |
| 決定 | 選んだ手法とその理由 |
| 検証 | 手法によって課題が解決されたか、結果はどうだったか |
| タグ | 自由入力の分類（例：業務／学習／創作） |
| 作成日・更新日 | 自動記録 |
| ステータス | 未着手 / 検証中 / 完了 / 保留 |

### 5.2 一覧・検索
- ステータス・タグ・期間でエントリを絞り込める。
- タイトル・本文の全文検索ができる。

### 5.3 振り返り
- 「検証」が未記入のまま一定期間経過したエントリを一覧表示し、振り返りを促す。
- 完了エントリを時系列で並べ、月ごとに簡易な件数サマリーを表示する。

### 5.4 エクスポート
- 全エントリを Markdown 形式でエクスポートできる（バックアップ・移植性のため）。

## 6. 非機能要件

- ローカル環境で完結して動作すること（外部サービスへの依存を必須としない）。
- データは自分の言葉で書いた自由記述が中心となるため、テキストとして可搬性の高い形式
  （Markdown ファイルまたはローカル DB）で保存すること。
- 個人利用規模（数千エントリ程度）で十分な性能が出ること。

## 7. データモデル（概略）

```
Entry
├─ id            : string (UUID)
├─ title         : string
├─ problem       : text   # 課題
├─ reason        : text   # なぜ解決するか
├─ options       : text   # 選択肢と違い
├─ decision      : text   # 選んだ理由
├─ verification  : text   # 検証結果
├─ tags          : string[]
├─ status        : enum(未着手, 検証中, 完了, 保留)
├─ createdAt     : datetime
└─ updatedAt     : datetime
```

## 8. 画面構成（概略）

1. **一覧画面**：エントリのリスト、検索・絞り込み UI
2. **詳細／編集画面**：5.1 の各項目を入力するフォーム
3. **振り返り画面**：未検証エントリと月次サマリー
4. **エクスポート画面**：Markdown 出力ボタン

## 9. 技術スタック（案）

- 個人用途・オフライン優先のため、ローカルファースト構成を想定。
- フロントエンド：軽量な SPA（例：Vite + 任意のフレームワーク）
- データ保存：ローカル DB（例：SQLite）またはファイルベース（Markdown + フロントマター）
- 上記は一案であり、実装フェーズで README の開発方針（複数選択肢を挙げて比較する）
  に従って改めて検討・決定する。

## 10. 開発方針との対応

本仕様書自体、README.md の「ソフトウェアにおける開発方針」に沿って以下のように整理した。

1. 課題を明らかにする → 本書 2 章
2. なぜ解決するかを論じる → 本書 3 章
3. 複数の選択肢と違いを論じる → 技術スタックは今後の実装時に複数案を比較する前提とする
4. 選択理由を論じる → 各機能は README の開発方針 1〜5 と対応づけて設計した
5. 課題が解決されたかを論じる → アプリ自体に「検証」欄を持たせ、機能として組み込んだ
6. 第三者コードを扱う際は十分に理解する → 実装時に適用
7. 「十分に理解」＝自分の言葉で説明できること → 本アプリの目的そのもの
8. 方針はアップデートされる → エントリは後から追記・修正可能な設計とした

## 11. 今後の展望

- タグの階層化・関連エントリのリンク機能
- 週次・月次の振り返りレポートの自動生成
- 必要になった時点でのマルチデバイス同期（初期スコープ外）
