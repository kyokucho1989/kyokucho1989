# 仕様書：フィジカルAI・スターターキット（Physical AI Starter Kit）

## 1. 概要

Physical AI Starter Kit（以下 PASK）は、フィジカルAI（実世界で身体を持って動く
AI：ロボットアーム、移動ロボット、ヒューマノイドなど）を初めて扱う人向けに、
「動かせる学習済みモデル」と「次に何をすればいいかを教えてくれるガイド」を
セットで提供するプラットフォームである。

シミュレーションから実機までの距離が遠いというフィジカルAI特有の高い参入障壁を、
モデル選択の迷いと手順の迷いの両方をなくすことで下げることを狙う。

## 2. 背景・課題

- フィジカルAIは近年急速に発展しているが、初心者にとっての参入障壁は依然として高い。
  - どのモデル（模倣学習、VLA＝Vision-Language-Actionモデル、強化学習方策など）を
    選べばよいか判断がつかない。
  - シミュレーション環境の構築（物理エンジン、センサー設定、ロボットモデルの用意）
    だけで挫折しやすい。
  - シミュレーションで動いたモデルを実機に移す「Sim-to-Real」の壁が高く、
    情報も断片的でまとまっていない。
  - 必要なハードウェア（GPU、対応ロボットキット、Jetson等のエッジデバイス）の
    選定基準がわかりにくい。
- README.md の開発方針にある「学びは入り口と出口から始める。成果物から逆算して
  はじめ、並行して体系的に理解する」という考え方は、フィジカルAI学習にこそ
  当てはまる。まず動くものを渡し、動かしながら理解を深めさせるべきである。

## 3. 目的

1. 初心者でも即座に試せる「学習済みモデル」を、用途別にカタログとして提供する。
2. 「次に何をすればよいか」を常に一つだけ示す、段階的なガイド機能を提供する。
3. シミュレーションから実機へのステップを、迷わず踏めるように可視化する。
4. 挫折しやすいポイント（環境構築、Sim-to-Real）を、テンプレート化して吸収する。

## 4. 対象ユーザー

- フィジカルAI・ロボティクスの実務経験がない個人開発者・学生・エンジニア。
- 機械学習の基礎知識はあるが、ロボット制御やシミュレーションには不慣れな層。
- 高価な実機を持たず、まずシミュレーションから始めたい人。

## 5. 提供価値（コンセプト）

> 「何を選べばいいか」と「次に何をすればいいか」を、常に一つに絞って提示する。

フィジカルAI初心者がつまずくのは技術力ではなく「選択肢の多さ」と「迷子になること」
である。PASK は選択肢を積極的に狭め、代わりに「まずこれをやってください」を
常に一つだけ提示することを設計の中心に置く。

## 6. 機能要件

### 6.1 モデルカタログ
- 用途別（つかむ／運ぶ／歩く／避ける、など）に整理された学習済みモデルを提供する。
- 各モデルには以下を必ず添える。
  - 何ができるか（一言）／何ができないか（一言）
  - 必要な最低ハードウェア（シミュレーションのみ可／実機が必要、など）
  - 難易度（★1〜3）
- 初心者には「まずこれ」という推奨モデルを1つだけ最初に提示する。

### 6.2 段階的ガイド（ラーニングパス）
以下の4段階で構成し、各段階の完了条件を明確にする。

| 段階 | 内容 | 完了条件 |
|---|---|---|
| 1. 動かす | 用意されたモデルをシミュレーターで動かすだけ | シミュレーション上でモデルが動作した |
| 2. 変える | パラメータやタスク対象を変えて挙動の変化を観察する | 挙動の違いを自分の言葉で説明できる |
| 3. 試す | 少量の自分のデータでモデルを微調整する | 微調整前後の違いを比較できる |
| 4. 出す | Sim-to-Real チェックリストに沿って実機（または対応キット）で動かす | 実機で最小タスクが1つ成功する |

- 各段階で「次にやること」を1つだけ画面に表示し、他の選択肢は折りたたんで隠す。
- 詰まった場合の「よくある失敗と対処」をステップごとに用意する。

### 6.3 シミュレーション環境テンプレート
- 主要な物理シミュレーター（例：Isaac Sim、MuJoCo 等）向けに、環境構築済みの
  テンプレートプロジェクトを配布し、ゼロからの構築を不要にする。
- 動作確認済みの組み合わせ（OS／GPU／シミュレーター／モデル）のみを提示し、
  非対応構成による事故を防ぐ。

### 6.4 Sim-to-Real 移行支援
- 実機移行前に確認すべき項目をチェックリスト化する
  （座標系の違い、センサーノイズ、安全停止手段の有無など）。
- 対応が確認された安価な入門用ロボットキットを推奨構成として明示する。

### 6.5 進捗の記録とコミュニティ共有
- 各段階の完了状況と、動作結果（動画・ログ）を記録する。
- 任意で進捗を共有し、他の初心者の記録を参照できるようにする（相互参照による学習）。

## 7. 非機能要件

- 初心者が「今何をすべきか」で迷わないよう、常時表示する選択肢は最大3つまでとする。
- シミュレーション環境はローカル実行を基本とし、外部クラウドは任意選択とする。
- モデルカタログは新規モデル追加が容易な構成にする（増え続けることを前提とする）。

## 8. データモデル（概略）

```
Model
├─ id            : string
├─ name          : string
├─ category      : enum(つかむ, 運ぶ, 歩く, 避ける, ...)
├─ difficulty     : int(1-3)
├─ minHardware    : enum(シミュレーションのみ, 実機推奨, 実機必須)
├─ canDo / cannotDo : text
└─ recommended    : boolean   # 「まずこれ」フラグ

LearningProgress
├─ userId         : string
├─ modelId        : string
├─ stage          : enum(動かす, 変える, 試す, 出す)
├─ completedAt    : datetime
├─ notes          : text      # 自分の言葉での説明・観察結果
└─ artifactUrl    : string    # 動作動画・ログへのリンク
```

## 9. 画面構成（概略）

1. **モデルカタログ画面**：用途別モデル一覧、推奨モデルの強調表示
2. **ラーニングパス画面**：現在の段階と「次にやること」を1つだけ表示
3. **環境構築画面**：動作確認済みテンプレートの選択・ダウンロード
4. **Sim-to-Realチェックリスト画面**：実機移行前の確認項目
5. **進捗・共有画面**：自分の記録と、共有された他者の記録の一覧

## 10. 技術スタック（案）

- モデル配信：既存の学習済みモデル（模倣学習／VLA系）をカタログ化して配信
- シミュレーション：Isaac Sim や MuJoCo 等、実績のあるシミュレーターとの連携を前提
- 実機連携：ROS2 等、広く使われるロボットミドルウェアとの接続を想定
- フロントエンド：段階的ガイドの UI に適した軽量な Web アプリ
- 上記は初期案であり、実装フェーズで複数選択肢を比較のうえ決定する。

## 11. 開発方針との対応

README.md の「ソフトウェアにおける開発方針」に沿って整理すると以下の通り。

1. 課題を明らかにする → 本書 2 章（選択肢の多さと迷子になりやすさが真の障壁）
2. なぜその課題を解決するかを論じる → フィジカルAIの参入障壁を下げ、学習を継続させるため
3. 複数の選択肢を挙げ、違いを論じる → シミュレーター・モデル形式は実装時に比較検討する
4. 手法を選択した理由を論じる → 「選択肢を狭め、次の一手を1つに絞る」設計を全機能の軸とした
5. 課題が解決されたかを論じる → ラーニングパスの完了条件と進捗記録で検証する
6. 第三者が出力したコードを扱うときは十分に理解して使う → 学習済みモデルの「できる／できない」を必ず明示する設計とした
7. 「十分に理解」とは自分の言葉で説明できること → 各段階の完了条件に「自分の言葉で説明できる」を含めた
8. 方針はアップデートされる → モデルカタログとガイドは追加・更新前提の構成とした

## 12. リスク・制約

- 実機は安全面のリスクを伴うため、Sim-to-Real 段階では安全停止手段の確保を必須条件とする。
- 対応ハードウェアの選定を誤ると初心者が離脱しやすいため、推奨構成は少数の実績あるものに絞る。

## 13. 今後の展望

- 初心者の進捗データをもとに、つまずきやすい段階を特定し、ガイドを改善する。
- コミュニティで共有された記録から、次の「まずこれ」推奨モデルを更新する仕組み。
- 対応ロボットキットの拡充（安価な入門機から中級機へのステップアップパス）。
